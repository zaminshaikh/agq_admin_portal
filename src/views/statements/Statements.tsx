import { useTranslation } from "react-i18next";

const Statements = () => {
    const { t } = useTranslation()
    return (
        <div>
            
        </div>
    );
};

export default Statements;